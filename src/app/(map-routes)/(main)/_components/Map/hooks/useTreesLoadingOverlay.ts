const useTreesLoadingOverlay = () => {
  // The trees loading shimmer is now rendered by the React SVG
  // TreesLoadingOverlay component using globe.gl screen coordinates.
};

export default useTreesLoadingOverlay;
