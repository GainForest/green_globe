// This store is no longer used for data fetching.
// Member data is now fetched directly in the Members component
// via the useOrganizationMembers hook (ATProto PDS).
// The file is kept to avoid breaking any potential imports.

export {};
